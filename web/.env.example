# Copy to .env.local for local dev; set the same vars in the Vercel dashboard.

# Supabase project (Ph_market)
SUPABASE_URL=https://takuhbkpagvhmxsncdls.supabase.co
# Service-role key — SERVER ONLY. Never prefix with NEXT_PUBLIC_, never expose.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: the "Get the app" button targets. Leave unset until published
# (the pages then show a "coming soon" note).
APP_PLAY_STORE_URL=
APP_STORE_URL=
