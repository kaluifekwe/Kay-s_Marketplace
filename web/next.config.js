/** @type {import('next').NextConfig} */
const nextConfig = {
  // Product/store images are plain <img> tags pointing at Supabase Storage, so
  // no next/image remote-pattern config is needed.
};

module.exports = nextConfig;
