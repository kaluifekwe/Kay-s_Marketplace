# Copy this file to `secrets.properties` (git-ignored) and fill in the real key.
# This is the CLIENT Google Maps key: restrict it to "Maps SDK for Android" and
# lock it to package com.kaysmarket.app + your upload-keystore SHA-1.
MAPS_API_KEY=YOUR_ANDROID_MAPS_SDK_KEY
